<div class="container-lg d-flex justify-content-center align-items-center row mx-auto">


  <h2 class="col-12 text-center font-weight-bold" style="margin-top: 4rem;">Profilini Düzenle </h2>
  <div class="col-lg-6">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, data:{turbo:false}})do |f| %>
    <div style= "display: none;">
      <%= render "devise/shared/error_messages", resource: resource %>
    </div>



      <div class="form-group text-center">
        <div class="row">
          <div class="col-12">
            <%= image_tag user_avatar(resource), class: "rounded-circle" %>
          </div>
          <div class="col-12 mt-3">
            <label class="btn btn-dark">
                Dosya Seçin
            <span style="display:none;">
              <%= f.file_field :avatar%>
            </span>
</label>
          </div>
        </div>
      </div>

  <div class="row justify-content-center">

        <button class="btn btn-info col-10" id="email-switch"> Email </button>
        <div class="field form-group col-12 mt-2">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control form-email", style:"display:none;"%>
        </div>


        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
        <% end %>

        <button class="btn btn-info col-10" id="password-switch"> Şifre </button>
        <div class="field form-group col-12 mt-2">
          <% if @minimum_password_length %>
           <%= f.password_field :password, placeholder:"Şifrenizi Giriniz (Min. ".concat(@minimum_password_length.to_s)+ " Karakter)", class:"form-control form-password", style:"display:none;"%>
         <% end %>
        </div>

        <div class="field form-group col-12">
          <%= f.password_field :password_confirmation, class:"form-control form-password", placeholder:"Şifrenizi Tekrar Giriniz", style:"display:none;"%>
        </div>

  </div>





  </div>
  <div class="col-lg-6 justify-content-end align-items-center">
      <div class="field form-group">
        <%= f.label "İsim" %>
        <%= f.text_field :name, class:"form-control", autocomplete:"name"%>
      </div>
      <div class="field form-group">
        <%= f.label "Soyisim" %>
        <%= f.text_field :surname, class:"form-control", autocomplete:"surname"%>
      </div>
      <div class="field form-group">
        <%= f.label "Telefon" %>
        <%= f.text_field :phone, class:"form-control", autocomplete:"phone"%>
      </div>
      <div class="field form-group">
        <%= f.label "Okul Numarası" %>
        <%= f.text_field :schoolNumber, class:"form-control", autocomplete:"schoolNumber"%>
      </div>
    </div>
    <div class="col-12 mt-4">
      <div class="field form-group">
        <%= f.label "*Güncellemek İçin Mevcut Şifrenizi Giriniz".titleize %>
        <%= f.password_field :current_password, class:"form-control"%>
      </div>
    <div class="d-flex justify-content-between" id= "edit-user-buttons">

    <div id="update-back-buttons">

        <div class="actions">
          <%= f.submit "Güncelle", class:"btn btn-info"%>
        </div>
      <% end %>

      <div>
         <%= link_to "Ana Sayfaya Geri Dön", root_path, class:"btn btn-dark mt-2"%>
      </div>


    </div>



     <%= button_to "Hesabımı Sil", registration_path(resource_name),  form: { data: { turbo_confirm: "Hesabınızı Silmek İstediğinize Emin Misiniz?" } }, method: :delete, class:"btn btn-danger mt-2", id:"user-cancel-btn"%>
    </div>

  </div>



  </div>


</div>
